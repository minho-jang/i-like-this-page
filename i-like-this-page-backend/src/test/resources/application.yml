# Test
spring:
  profiles:
    active: test

---
spring:
  profiles: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
  jwt:
    issuer: i-like-this-page
    secret: secret
